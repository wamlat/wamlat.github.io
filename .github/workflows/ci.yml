name: Test Workflow Trigger # New name to avoid confusion

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: Test Run
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Echo message
        run: echo "Custom workflow is running!"
